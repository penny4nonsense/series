// (intentionally empty during development)
//
// sbt-pgp is only needed for the final artifact-signing step at release
// time. It is added back just before publishing — see RELEASING.md.
// Central Portal publishing itself is built into sbt 1.11+.
